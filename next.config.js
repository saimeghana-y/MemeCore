/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['lh3.googleusercontent.com','firebasestorage.googleapis.com', 'platform-lookaside.fbsbx.com', 'ipfs.io', 'gateway.lighthouse.storage', 'cdn.img.ly']
    },
}

module.exports = nextConfig